# TODO: Install vite-plugin-imagemin for WebP conversion

## Steps:

- [x] 1. Install vite-plugin-imagemin package
- [x] 2. Update vite.config.ts to use vite-plugin-imagemin
- [ ] 3. Remove vite-plugin-image-optimizer from package.json (optional - for cleanup)
- [ ] 4. Test the build to verify WebP conversion works

## Notes:

- vite-plugin-imagemin converts JPG/PNG to WebP at build time
- Configuration includes gifscrot, mozjpeg, pngquant, svgo, etc. plugins
